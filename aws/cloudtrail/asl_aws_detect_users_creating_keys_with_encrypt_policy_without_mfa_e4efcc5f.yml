title: Asl Aws Detect Users Creating Keys With Encrypt Policy Without Mfa
id: asl-aws-detect-users-creating-keys-with-encrypt-policy-without-mfa
status: experimental
description: |
  Detects asl aws detect users creating keys with encrypt policy without mfa activity which may indicate malicious behavior.
  Category: AWS_CloudTrail | Source: splunk
references:
- https://sigmahq.io/docs/basics/rules.html
- https://attack.mitre.org/
author: REVSOC
date: 20/10/2025
tags:
- attack.impact
- attack.t1486
logsource:
  product: aws
  service: cloudtrail
  definition: Detects Asl Aws Detect Users Creating Keys With Encrypt Policy Without Mfa based on the configured Sigma detection logic.
detection:
  selection:
    eventSource: cloudtrail.amazonaws.com
    eventName: AslAwsDetectUsersCreatingKeysWithEncryptPolicyWithoutMfa
  condition: selection
falsepositives:
- Legitimate administrative activity
- Authorized security testing
- Known good software behavior
level: medium
# notes:
  #  - This rule detects Asl Aws Detect Users Creating Keys With Encrypt Policy Without Mfa.
  #  - Logsource follows the AWS reference style: product=aws, service/category=cloudtrail.
  #  - Condition used by the rule: selection.
  #  - Tune allowlists and environment-specific filters before production deployment.
# api_parameters:
  # - api: Source Log Collection
    # parameters:
      # - name: product
        # type: String
        # description: Log product or platform expected by this Sigma rule
      # - name: service_or_category
        # type: String
        # description: Service/category used to route events for this rule
# cli_examples:
  # - description: "Validate rule syntax with Sigma tooling"
    # command: |
      # sigma check asl_aws_detect_users_creating_keys_with_encrypt_policy_without_mfa.yml
  # - description: "Convert rule to a backend SIEM query"
    # command: |
      # sigma convert -t splunk asl_aws_detect_users_creating_keys_with_encrypt_policy_without_mfa.yml
# siem_examples:
  # - siem: Splunk
    # query: |
      # index=<your_index> sourcetype=<mapped_sourcetype> | search <translated Sigma logic>
  # - siem: Elastic
    # query: |
      # event.dataset:<mapped_dataset> AND <translated Sigma logic>
  # - siem: QRadar
    # query: |
      # SELECT * FROM events WHERE <translated Sigma logic>
# remediation:
  # - step: 1
    # description: "Confirm whether the activity is expected or authorized."
  # - step: 2
    # description: "Identify the user, host, source IP, destination, process, and related timeline."
  # - step: 3
    # description: "Contain affected accounts or systems if the activity is suspicious."
  # - step: 4
    # description: "Tune false positives using known-good users, hosts, services, or maintenance windows."
# security_notes:
  # - "Severity level: medium"
  # - "Validate against real logs before enabling alerting in production."
  # - "Track TP/FP outcomes to prevent useful rules from being disabled due to noise."
